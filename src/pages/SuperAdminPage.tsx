// Stubbed: multi-tenant + super-admin removed for the v1 launch.
// Kept as a redirect so any stale links go home.
import { Navigate } from "react-router-dom";
export const SuperAdminPage = () => <Navigate to="/" replace />;
